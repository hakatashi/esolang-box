#!/bin/bash

infile=$(realpath "$@")

mkdir -p /tmp/code
if [[ "$infile" == *.zip ]]; then
	unzip "$infile" -d /tmp/code
else
	cp -sfr "$infile" /tmp/code
fi

cd ~/interpreters/Folders/Rottytooth.Esolang.Folders/bin/Release && mono Folders.exe /tmp/code /s /b
