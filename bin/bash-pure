#!/bin/bash

infile=$(realpath "$@")
sudo cp -f "$infile" /opt/jails/pure-bash/code.bash
cat - | sudo jk_chrootlaunch -j /opt/jails/pure-bash -x /bin/bash /code.bash
sudo rm /opt/jails/pure-bash/code.bash
