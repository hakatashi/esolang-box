#!/bin/bash

# All INTARCAL code should have prefix .i
infile=$(realpath "$@")
ln -sf "$infile" /tmp/code.i

~/interpreters/intercal/intercal-0.30/ick /tmp/code.i
cat - | /tmp/code

rm /tmp/code.i /tmp/code
