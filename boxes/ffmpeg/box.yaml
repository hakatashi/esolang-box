name: FFmpeg
link: "https://ffmpeg.org/"
parent: base
# Broken after ffmpeg version bump
disabled: true
disabled_after: 2.5.0
tests:
  hello:
    file: hello.ffmpeg
    expected: "Hello, World!"
  cat:
    file: cat.ffmpeg
    stdin: "meow! meW12"
    expected: "meow! meW12"
credits:
  hello.ffmpeg: Original
  cat.ffmpeg: Original
